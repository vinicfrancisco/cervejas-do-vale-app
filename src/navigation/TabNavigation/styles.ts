import styled from 'styled-components/native';

export const BottomTabBackground = styled.View`
  background-color: ${({ theme }) => theme.colors.softBlack};
  bottom: 0;
  left: 0;
  position: absolute;
  right: 0;
  top: 0;
`;
